# Path to the SQLite database file (created on first run).
DATABASE_FILE=./untangle.db

# Port the Fastify API listens on.
PORT=3001

# Origin allowed to call the API in dev (the Vite client).
CLIENT_ORIGIN=http://localhost:5173
